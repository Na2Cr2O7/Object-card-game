# This project was generated with 0.3.22 using template: https://github.com/beeware/briefcase-template@v0.3.22
[tool.briefcase]
project_name = "ObjectCardGame"
bundle = "com.example"
version = "0.0.1"
url = "https://example.com/objectcardgame"
license.file = "LICENSE"
author = "aaaaa"
author_email = "aaaaa@example.com"

[tool.briefcase.app.objectcardgame]
icon = "ccb"
formal_name = "ObjectCardGame"
description = "aaaaa"
long_description = """More details about the app should go here.
"""
sources = [
    "src/objectcardgame",
]
test_sources = [
    "tests",
]
console_app = true

requires = [
    "colorama",
]
test_requires = [
    "pytest",
]

[tool.briefcase.app.objectcardgame.macOS]
universal_build = true
requires = [
    # Add your macOS-specific app requirements here
]

[tool.briefcase.app.objectcardgame.linux]
requires = [
    # Add your Linux-specific app requirements here
]

[tool.briefcase.app.objectcardgame.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.objectcardgame.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.objectcardgame.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.objectcardgame.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.objectcardgame.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "24.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.objectcardgame.windows]
requires = [
    # Add your Windows-specific app requirements here
]

# Mobile deployments
[tool.briefcase.app.objectcardgame.iOS]
supported = false

[tool.briefcase.app.objectcardgame.android]
supported = false

# Web deployments
[tool.briefcase.app.objectcardgame.web]
supported = false

